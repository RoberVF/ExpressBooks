<!DOCTYPE html>
<html lang="en">

<head>
    <% include partials/header.ejs %>
    <!--Incluir el header-->
    <link rel="stylesheet" href="estilos.css">
    <title>ExpressBooks</title>
</head>

<body class="bg-light">
    <!--Archivo copiado de bootstrap 4(https://getbootstrap.com/docs/4.3/components/navbar/)-->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="/">ExpressBooks</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse " id="navbarNav">
            <ul class="navbar-nav ml-auto" class="ul">
                <!--ml-auto mueve los <li> hacia la derecha-->
                <li class="nav-item border border-primary rounded-pill mr-3 p-1">
                    <a class="nav-link text-light" href="/new-entry">New book</a>
                </li>
                <li class="nav-item border border-primary rounded-pill mr-2 p-1">
                    <a class="nav-link text-light" href="/about">About</a>
                </li>
            </ul>
        </div>
    </nav>
    <div class="container p-4">
        <div class="row">
                <% books.forEach(function(book){ %> <!--Por cada libro que se recibe del servidor se genera una tarjeta-->   
            <a href="<%= book.link%>">
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-header">
                            <%= book.title %> / <%= book.author%>
                        </div>
                        <img src="<%= book.image %>" class="card-img-top" alt="Imagen del libro">
                        
                        <div class="card-body">
                            <p><%= book.description %></p>
                        </div>
                        <div class="card-footer">
                            <a href="/delete/<%= book.id %>" class="btn btn-danger">
                                Delete
                            </a>
                        </div>
                    </div>
                </div>
                <%})%>
            </a>
        </div>
    </div>
</body>

</html>